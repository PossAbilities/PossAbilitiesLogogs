feat: Implement Admin-configurable Hero Image Banner for main portal

Added functionality to the Admin Dashboard allowing administrators to toggle
between the existing "How are you feeling today?" widget and a new
promotional Hero Image Banner on the main portal page.

- Created a SQL migration to add a `hero_settings` table to Supabase.
- Updated `DataProvider` to fetch and upsert `heroSettings` state.
- Added a "Homepage Hero Banner" settings section to `/admin/page.tsx`.
- Updated the main portal page (`/page.tsx`) to conditionally render the
  Hero Image Banner or the Feeling Widget based on the configured state.
